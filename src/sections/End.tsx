import * as React from 'react'
import SectionTitle from 'components/SectionTitle'
import { EndSpace } from 'components/Styled'

class End extends React.Component {
	public render() {
		return (
			<div>
				<SectionTitle />
				<EndSpace />
			</div>
		)
	}
}

export default End
